interface TransactionStatusResponse {
    success: boolean;
    message: string;
    transaction: any;
}
declare function getTransactionStatus(apiKey: string, reference: string): Promise<TransactionStatusResponse>;
export = getTransactionStatus;
