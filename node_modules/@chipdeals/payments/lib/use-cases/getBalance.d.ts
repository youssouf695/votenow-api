interface BalanceResponse {
    success: boolean;
    message: string;
    balances: {
        countryCode: string;
        operator: string;
        amount: number;
        currency: string;
    }[];
}
declare function getBalance(apiKey: string): Promise<BalanceResponse>;
export = getBalance;
