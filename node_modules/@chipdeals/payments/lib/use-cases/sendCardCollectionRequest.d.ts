interface CollectionResponse {
    success: boolean;
    message: string;
    payment: any;
}
declare function sendCardCollectionRequest({ apiKey, encApiKey, amount, currency, fee, merchantSupportFee, phoneNumber, firstName, lastName, title, email, address, locality, district, postalCode, noCard, cvv, expiry, webhookUrl, otp, }: {
    apiKey: string;
    encApiKey: string;
    amount: number;
    currency: string;
    fee: number;
    merchantSupportFee: boolean;
    phoneNumber: string;
    firstName: string;
    lastName: string;
    title: string;
    email: string;
    address: string;
    locality: string;
    district: string;
    postalCode: string;
    noCard: string;
    cvv: string;
    expiry: string;
    webhookUrl: string;
    otp: string;
}): Promise<CollectionResponse>;
export = sendCardCollectionRequest;
