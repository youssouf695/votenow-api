interface CollectionResponse {
    success: boolean;
    message: string;
    payment: any;
}
declare function sendCollectionRequest({ apiKey, amount, currency, fee, merchantSupportFee, phoneNumber, firstName, lastName, webhookUrl, otp, }: {
    apiKey: string;
    amount: number;
    currency: string;
    fee: number;
    merchantSupportFee: boolean;
    phoneNumber: string;
    firstName: string;
    lastName: string;
    webhookUrl: string;
    otp: string;
}): Promise<CollectionResponse>;
export = sendCollectionRequest;
