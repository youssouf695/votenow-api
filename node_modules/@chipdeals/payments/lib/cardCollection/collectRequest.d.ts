import CollectionExecution = require('./collectionExecution');
declare class CollectRequest {
    private _enableLogs;
    private _apiKey;
    private _secretKey;
    private _amount;
    private _currency;
    private _fee;
    private _merchantSupportFee;
    private _phoneNumber;
    private _firstName;
    private _lastName;
    private _title;
    private _email;
    private _address;
    private _locality;
    private _district;
    private _postalCode;
    private _noCard;
    private _cvv;
    private _expiry;
    private _webhookUrl;
    private _otp;
    amount(amount: number): CollectRequest;
    currency(currency: string): CollectRequest;
    setFee(fee: number, userSupportAllFees?: boolean): CollectRequest;
    phoneNumber(phoneNumber: string): CollectRequest;
    firstName(firstName: string): CollectRequest;
    lastName(lastName: string): CollectRequest;
    title(title: string): CollectRequest;
    email(email: string): CollectRequest;
    address(address: string): CollectRequest;
    locality(locality: string): CollectRequest;
    district(district: string): CollectRequest;
    postalCode(postalCode: string): CollectRequest;
    noCard(noCard: string): CollectRequest;
    cvv(cvv: string): CollectRequest;
    expiry(expiry: string): CollectRequest;
    webhook(webhookUrl: string): CollectRequest;
    create(onCreateCallback?: (transactionReference: string) => any): CollectionExecution;
    constructor(apiKey: string, secretKey: string, enableLogs: boolean);
}
export = CollectRequest;
